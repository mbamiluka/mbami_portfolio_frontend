import React, { useEffect, useState } from "react";

const url = process.env.REACT_APP_MBAMI_PORTFOLIO_BACKEND_URL_DEVELOPMENT;

const useGetProficiencies = (url) => {
}

export default useGetProficiencies;
